services:
  env-hub:
    build: .
    restart: unless-stopped
    ports:
      - "9800:9800"
    environment:
      - ADMIN_TOKEN=${ADMIN_TOKEN}
      - LISTEN_ADDR=:9800
    volumes:
      - ./data:/data
