services:
  env-hub:
    image: ghcr.io/xmzo/env-hub:latest
    restart: unless-stopped
    ports:
      - "9800:9800"
    environment:
      - ADMIN_TOKEN=${ADMIN_TOKEN}
      - LISTEN_ADDR=:9800
      - TURNSTILE_KEYS=${TURNSTILE_KEYS}
    volumes:
      - ./data:/data
